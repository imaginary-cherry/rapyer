---
phase: 02-traverse-sf-held-references-server-side-and-re-arm-children
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/documentation/special-fields/ttl-cascade.md
autonomous: true
requirements: [CASF-10]

must_haves:
  truths:
    - "D-04: full docs (not the bare minimum) — the TTL Cascade documentation states that RedisSet[ForeignKey[T]] and RedisPriorityQueue[ForeignKey[T]] fields participate in cascade the same way inline FK fields do, with a coverage matrix, a worked example, and the fakeredis divergence note."
    - "A coverage-matrix table enumerates all five cascade-eligible shapes: direct FK, collection-of-FK, nested-submodel FK, RedisSet[Reference[T]], RedisPriorityQueue[Reference[T]]."
    - "The docs carry a worked example showing a RedisSet/RedisPriorityQueue cascade parent and its resulting per-child refresh."
    - "The docs explicitly restate the fakeredis/real-Redis divergence for the SF-held-ref case (real Redis 7+ traversal; fakeredis root-own-EXPIRE fallback, no traversal)."
  artifacts:
    - path: "docs/documentation/special-fields/ttl-cascade.md"
      provides: "New coverage-matrix table + worked RedisSet/PriorityQueue cascade example + explicit SF-held-ref fakeredis-divergence note"
  key_links:
    - from: "docs/documentation/special-fields/ttl-cascade.md"
      to: "tests/models/cascade_types.py CascadeSetRefParent/CascadePQRefParent shapes"
      via: "worked example mirrors the real fixture field declarations (RedisSet[Reference[T]] with CascadeTTL())"
      pattern: "RedisSet\\[Reference"
---

<objective>
Extend the TTL Cascade documentation (per D-04, "full docs, not the bare minimum") to state that SF-held references — `RedisSet[ForeignKey[T]]` and `RedisPriorityQueue[ForeignKey[T]]` — participate in cascade, add a coverage-matrix table enumerating all five cascade-eligible shapes, add a worked SF-held-ref example, and restate the fakeredis/real-Redis divergence explicitly for this new shape.

Purpose: Close CASF-10 — the doc site and docstrings must not undersell what the feature now does; a user reading `ttl-cascade.md` today would have no reason to suspect a `RedisSet`/`RedisPriorityQueue` field can cascade at all.
Output: An updated `docs/documentation/special-fields/ttl-cascade.md` with a new coverage-matrix table, a worked example, and an explicit divergence note.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-traverse-sf-held-references-server-side-and-re-arm-children/02-CONTEXT.md
@docs/documentation/special-fields/ttl-cascade.md
@tests/models/cascade_types.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add coverage matrix, worked example, and divergence note to ttl-cascade.md</name>
  <files>docs/documentation/special-fields/ttl-cascade.md</files>
  <read_first>
    docs/documentation/special-fields/ttl-cascade.md (full file — no literal "coverage matrix" table exists today; the doc currently documents shapes in prose only)
    tests/models/cascade_types.py (CascadeSetRefParent/CascadePQRefParent — the real field declarations the worked example should mirror)
    .planning/phases/02-traverse-sf-held-references-server-side-and-re-arm-children/02-RESEARCH.md (Open Question 2 — recommends adding a small literal table since none exists)
  </read_first>
  <action>
    Insert a new "## Cascade-Eligible Shapes" section immediately after the existing "## Enabling Cascade" section (before "## Precedence"), containing:

    1. A markdown table with columns `Shape | Example | Cascade-eligible` enumerating all five shapes: direct FK field (`Reference[Author]`), collection-of-FK (`list[Reference[Author]]` / `dict[K, Reference[Author]]`), nested-submodel FK (an inline sub-model whose own field is FK-annotated), `RedisSet[Reference[Author]]`, `RedisPriorityQueue[Reference[Author]]` — marking all five as cascade-eligible via the same `field > global > off` precedence rule already documented.
    2. A short worked example showing a `RedisSet[Reference[Author]]` field annotated with `CascadeTTL()` (mirroring `CascadeSetRefParent`'s real declaration), a member added via `await parent.refs.aadd(author.key)`, and the resulting behavior when `await parent.asave()` or `await parent.aset_ttl(ttl, cascade=True)` fires: every member of the set is re-armed to its own `Meta.ttl`, same as an inline collection-of-FK field.
    3. Extend the existing "Requires real Redis 7+ (Redis Functions)" warning admonition near the top of the file with one explicit sentence: SF-held-ref cascade (RedisSet/RedisPriorityQueue members) follows the identical real-Redis-7+/fakeredis divergence as inline cascade — on fakeredis, the SF container's own key still refreshes via plain EXPIRE, but its members are never followed (no traversal), matching the general "edges are not followed" statement already there.

    Do not remove or restructure any existing section; this is additive documentation only.
  </action>
  <verify>
    <automated>grep -c "Cascade-Eligible Shapes" docs/documentation/special-fields/ttl-cascade.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Cascade-Eligible Shapes" docs/documentation/special-fields/ttl-cascade.md` == 1.
    - The new table lists exactly 5 rows (one per shape) under a `Shape | Example | Cascade-eligible` header.
    - `grep -c "RedisSet\[Reference" docs/documentation/special-fields/ttl-cascade.md` >= 1 (worked example present).
    - `grep -c "RedisPriorityQueue" docs/documentation/special-fields/ttl-cascade.md` >= 1.
    - The existing "Requires real Redis 7+" admonition block still exists (unmodified aside from the one added sentence) — `grep -c "Requires real Redis 7" docs/documentation/special-fields/ttl-cascade.md` == 1.
  </acceptance_criteria>
  <done>ttl-cascade.md documents all five cascade-eligible shapes in a literal coverage-matrix table, includes a worked RedisSet cascade example, and explicitly restates the fakeredis divergence for SF-held-ref cascade.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None new | Documentation-only change; no runtime code, no trust boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05 | N/A | Docs-only file | accept | No production code, no test execution surface, no threat |
</threat_model>

<verification>
- `docs/documentation/special-fields/ttl-cascade.md` builds/renders as valid Markdown (no broken table syntax — visually inspect the new table's pipe alignment).
- All acceptance criteria greps above pass.
</verification>

<success_criteria>
- CASF-10 is closed: the cascade documentation states SF-held references participate in cascade, with a coverage matrix covering all five shapes and a worked example.
</success_criteria>

<output>
Create `.planning/phases/02-traverse-sf-held-references-server-side-and-re-arm-children/02-03-SUMMARY.md` when done
</output>

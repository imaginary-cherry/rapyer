---
phase: quick-260714-l0p
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/unit/cascade/conftest.py
  - tests/integration/lst/test_redis_list_remove_range.py
  - tests/integration/pipeline/test_pipeline_noscript_recovery.py
autonomous: true
requirements: []
must_haves:
  truths:
    - "No production code under rapyer/ changes; every edit is confined to test files. The deliberate, tested init_rapyer() contract (init_rapyer() without cascade_ttl resets every model's Meta.cascade_ttl to None, per tests/unit/cascade/test_init_rapyer_cascade_ttl.py) is preserved untouched."
    - "The cascade unit fixtures re-establish each model's class-declared Meta.cascade_ttl during setup from a snapshot captured at conftest import time (before any init_rapyer() can wipe it), and restore the pre-fixture value on teardown -- so the 7 cascade unit tests pass regardless of whether a prior test called init_rapyer()."
    - "The two integration tests save their model while scripts are loaded, THEN clear/flush scripts, so the expected error (ScriptsNotInitializedError / PersistentNoScriptError) fires inside the pytest.raises block rather than during Arrange (asave now always runs the cascade Lua script via refresh_ttl)."
    - "Full suite passes with zero failures: REDIS_DB=0 python -m pytest tests -q -p no:randomly (real Redis Stack on localhost:6370)."
  artifacts:
    - path: "tests/unit/cascade/conftest.py"
      provides: "Import-time snapshot of each CASCADE_PLANNER_MODELS class's declared Meta.cascade_ttl; both setup_fake_redis_for_cascade_models and setup_fake_redis_for_cascade_apply re-apply it on setup and restore the pre-fixture value on teardown."
      contains: "cascade_ttl"
    - path: "tests/integration/lst/test_redis_list_remove_range.py"
      provides: "test_remove_range_raises_scripts_not_initialized_error_... saves the model first, then clears _REGISTERED_SCRIPT_SHAS inline (with restore) so only the pipeline remove_range hits the missing script."
    - path: "tests/integration/pipeline/test_pipeline_noscript_recovery.py"
      provides: "test_pipeline_raises_persistent_noscript_error_... drops the flush_scripts fixture, saves the model, then SCRIPT FLUSHes manually before the pytest.raises block."
  key_links:
    - from: "tests/unit/cascade/conftest.py fixtures"
      to: "rapyer/cascade/planner.py _classify_edge"
      via: "planner reads model_cls.Meta.cascade_ttl for blanket edges; fixture guarantees that value is the class-declared CascadeTTL during the cascade tests"
      pattern: "Meta.cascade_ttl"
---

<objective>
Fix the 9 tests failing on PR #283 (branch `cascade-ttl-full-review`). All are test-isolation / test-ordering defects exposed when the whole suite runs together; the production behavior is correct and deliberate. Fixes are test-only — no `rapyer/` changes.
</objective>

<root_cause>
**Cause B (7 cascade unit failures — test_cascade_apply_lua.py, test_cascade_plan_table.py):**
`init_rapyer()` authoritatively sets `model.Meta.cascade_ttl = cascade_ttl` for every registered model (default `None`) — a deliberate, tested contract (`test_init_rapyer_cascade_ttl.py::test_init_rapyer_without_cascade_ttl_resets_prior_value_to_none_sanity`). Any prior `init_rapyer()` call in the suite permanently wipes each model's class-declared `Meta.cascade_ttl` (e.g. `CascadeBlanketRoot` → `CascadeTTL(depth=2)`). The cascade fixtures only snapshot/restore `redis`/`is_fake_redis`, so the blanket cascade config is gone → planner produces 0 edges → cascade traversal stops early.

**Cause A (2 integration failures):**
`refresh_ttl` now ALWAYS runs the cascade Lua script, so `asave()` on a TTL model needs scripts loaded. Two tests clear/flush scripts BEFORE `asave()`, so the expected error fires during Arrange instead of inside `pytest.raises`.
</root_cause>

<task id="1">
**Fix cascade unit fixtures (Cause B) — tests/unit/cascade/conftest.py**

- After the `CASCADE_PLANNER_MODELS` list, capture an import-time snapshot:
  `_DECLARED_CASCADE_TTL = {model: model.Meta.cascade_ttl for model in CASCADE_PLANNER_MODELS}`
  (conftest import happens during collection, before any test runs `init_rapyer()`, so values are pristine.)
- In BOTH `setup_fake_redis_for_cascade_models` and `setup_fake_redis_for_cascade_apply`:
  - Extend the per-model `original_clients` snapshot tuple to also record `model.Meta.cascade_ttl`.
  - In the setup loop, after wiring redis, set `model.Meta.cascade_ttl = _DECLARED_CASCADE_TTL[model]` to re-establish the class-declared blanket config.
  - In the teardown loop, restore `model.Meta.cascade_ttl` alongside `redis`/`is_fake_redis`.
- `reset_meta_freeze` (autouse, tests/conftest.py) already unfreezes Meta before these fixtures run, so the assignments won't hit MetaFrozenError. Do not change it.

verify: `REDIS_DB=0 python -m pytest tests/unit/test_init_rapyer.py tests/unit/cascade -q -p no:randomly` → all pass.
done: cascade unit tests pass even when init_rapyer tests run first.
</task>

<task id="2">
**Fix integration tests (Cause A)**

`tests/integration/lst/test_redis_list_remove_range.py::test_remove_range_raises_scripts_not_initialized_error_when_init_rapyer_not_called_error`:
- Drop the `clear_script_state` fixture param. Save the model first (scripts loaded), then inline: snapshot `_REGISTERED_SCRIPT_SHAS`, `.clear()`, wrap the `pytest.raises(ScriptsNotInitializedError)` + `apipeline()/remove_range` in try/finally, restore the snapshot in `finally`. Keep the existing module-level `clear_script_state` fixture if other tests use it; otherwise it may remain unused (do not remove shared fixtures relied upon elsewhere).

`tests/integration/pipeline/test_pipeline_noscript_recovery.py::test_pipeline_raises_persistent_noscript_error_when_scripts_keep_failing_error`:
- Drop the `flush_scripts` fixture param; keep `disable_noscript_recovery`. Add `real_redis_client` param (or use `model.Meta.redis`) for the client handle. Save the model, then `await real_redis_client.execute_command("SCRIPT", "FLUSH")`, then run the existing `pytest.raises(PersistentNoScriptError)` block.

verify: `REDIS_DB=0 python -m pytest tests/integration/lst/test_redis_list_remove_range.py tests/integration/pipeline/test_pipeline_noscript_recovery.py -q -p no:randomly` → all pass.
done: both target tests pass; the error is raised inside pytest.raises, not Arrange.
</task>

<task id="3">
**Full-suite verification**

Run: `REDIS_DB=0 python -m pytest tests -q -p no:randomly`
Expect: 0 failures (previously 9 failed). Then `black --check` and `ruff check` on the three touched files.
done: full suite green, formatting clean.
</task>

---
phase: quick-260707-v5y
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - rapyer/cascade/planner.py
  - tests/unit/cascade/test_cascade_planner.py
  - tests/unit/cascade/test_cascade_apply_lua.py
autonomous: true
requirements: []
must_haves:
  truths:
    - "CascadePlanner no longer exposes atraverse/_mget/_walk_edges (dead-on-production-path code is gone)"
    - "build_cascade_plan / validate_cascade_ttl_targets / _static_walk_fk_edges still work unmodified and their existing tests still pass"
    - "Every behavioral scenario previously proven only via CascadePlanner.atraverse (extend-past-shallower-ancestor, blanket decrement, blanket opt-out, shape-3 nested-submodel budget interaction, diamond dedup, self-reference cycle safety) is proven against the real registered cascade_ttl_apply Lua script"
    - "The 3 existing Lua/planner parity tests assert against explicit hand-derived key sets, not a live CascadePlanner().atraverse(...) oracle"
  artifacts:
    - path: "rapyer/cascade/planner.py"
      provides: "CascadePlanner reduced to __init__/_next_hop/_resolve_target_cls/_unwrap_nested_model_cls plus the untouched module-level build_cascade_plan/validate_cascade_ttl_targets/_static_walk_fk_edges/_static_walk_special_suffixes/_unwrap_relational_target"
    - path: "tests/unit/cascade/test_cascade_apply_lua.py"
      provides: "All atraverse-derived scenarios re-proven via the registered Lua script; no import of CascadePlanner remains"
  key_links:
    - from: "tests/unit/cascade/test_cascade_apply_lua.py"
      to: "rapyer/scripts/lua/cascade/apply.lua"
      via: "arun_sha(..., CASCADE_TTL_APPLY_SCRIPT_NAME, ...) against fake_redis_client"
      pattern: "arun_sha"
---

<objective>
Delete the dead-on-production-path runtime BFS traversal (`CascadePlanner.atraverse`, `_mget`, `_walk_edges`) from `rapyer/cascade/planner.py` -- nothing in the shipped code path calls them; `build_cascade_plan` reaches its targets exclusively through the separate, already-correct `_static_walk_fk_edges` static walker. Rework the test suite so no coverage is lost: delete the now-orphaned `test_cascade_planner.py` (all 17 tests exercise only the deleted method) and port its behavioral scenarios into `test_cascade_apply_lua.py` as real assertions against the registered `cascade_ttl_apply` Lua script, replacing the "run `CascadePlanner().atraverse()` as an oracle, then compare" pattern with explicit, hand-derived expected key sets.

Purpose: Remove ~85 lines of runtime code with zero production callers, its own docstring's stale self-description, and the maintenance burden of a full async-BFS twin of the Lua walker -- while keeping every scenario it protected against regression.
Output: Trimmed `rapyer/cascade/planner.py`; `test_cascade_planner.py` deleted; `test_cascade_apply_lua.py` gains 2 new ported scenarios (blanket opt-out, nested-submodel zero-hop budget non-consumption) and has its 3 existing parity tests repointed to hardcoded expected sets.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@.planning/STATE.md

<interfaces>
Kept surface of rapyer/cascade/planner.py (do not change signatures):

class CascadePlanner:
    def __init__(self, field_attr="_cascade_ttl_fields", global_attr="cascade_ttl")
    def _next_hop(self, model_cls, field_name, remaining_budget, established) -> tuple[bool, int | None]
    def _resolve_target_cls(self, model_cls, field_name) -> Any | None
    def _unwrap_nested_model_cls(self, annotation) -> Any | None

def build_cascade_plan(models: list[type["AtomicRedisModel"]]) -> dict[str, dict]
def validate_cascade_ttl_targets(plan: dict[str, dict]) -> None
def _static_walk_fk_edges(planner, model_cls, parent_path, fks) -> None
def _static_walk_special_suffixes(model_cls, parent_path="") -> list[str]
def _unwrap_relational_target(annotation) -> Any | None

Delete entirely from CascadePlanner:
- async def _mget(self, entries) -> dict[str, Any]
- async def atraverse(self, root_key, root_cls) -> list[str]
- def _walk_edges(self, model_cls, dump, remaining_budget, established)

rapyer/cascade/__init__.py re-exports CascadePlanner unchanged -- do not touch that file.
</interfaces>

Lua fixture depth semantics needed to hand-derive expected key sets (from rapyer/scripts/lua/cascade/apply.lua's `next_hop`): an explicit per-field CascadeTTL override REFRESHES the child's budget to its own depth regardless of the inherited budget; a blanket Meta.cascade_ttl sets the budget on the first (not-yet-established) hop into a fresh subtree and DECREMENTS it on every subsequent established hop; a nested inline submodel field (shape 3) is a zero-hop recursion that never calls next_hop for the nesting field itself, so it never consumes the inherited budget.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete dead runtime traversal from CascadePlanner and update its docstring</name>
  <files>rapyer/cascade/planner.py</files>
  <action>
    In rapyer/cascade/planner.py, delete the entire `async def _mget(...)` method, the entire `async def atraverse(...)` method, and the entire `def _walk_edges(...)` method from `CascadePlanner` (currently the block spanning roughly lines 102-207 of the file). Keep `__init__`, `_next_hop`, `_resolve_target_cls`, `_unwrap_nested_model_cls` untouched -- `build_cascade_plan` via `_static_walk_fk_edges` still calls `_next_hop`/`_resolve_target_cls`/`_unwrap_nested_model_cls` directly and must keep working unmodified. Do not touch `_static_walk_fk_edges`, `_static_walk_special_suffixes`, `build_cascade_plan`, `validate_cascade_ttl_targets`, or `_unwrap_relational_target` -- none of them call the deleted methods (confirmed: `_static_walk_fk_edges` only mirrors `_walk_edges` in its own docstring/comment, it never calls it, and a repo-wide grep shows no other caller of `atraverse`/`_walk_edges`/`_mget` outside this file and the two test files handled in Task 2).

    Rewrite the `CascadePlanner` class docstring (currently describes `atraverse` as "operation-agnostic FK-graph walker") to describe its reduced role: a small bundle of static edge-classification helpers (`_next_hop`/`_resolve_target_cls`/`_unwrap_nested_model_cls`) that `build_cascade_plan`'s `_static_walk_fk_edges` uses to build the baked, per-class plan table consumed by the Lua `cascade_ttl_apply` script -- it never touches Redis and never hydrates a model. Preserve the existing EXT-01 forward-looking note (a future `CascadeDelete` strategy can reuse `_next_hop` unmodified by pointing `field_attr`/`global_attr` at `_cascade_delete_fields`/`Meta.cascade_delete`) since that rationale still applies to the surviving helpers.

    Update the docstring on the module-level `_static_walk_fk_edges` function, which currently says it mirrors "`CascadePlanner._walk_edges`" -- reword to state it performs the same shape-1/2/3 static edge classification (no runtime dump involved), since the method it used to mirror no longer exists.
  </action>
  <verify>
    <automated>cd /Users/yedidyakfir/Documents/rapyer && python -c "from rapyer.cascade.planner import CascadePlanner, build_cascade_plan, validate_cascade_ttl_targets; assert not hasattr(CascadePlanner, 'atraverse'); assert not hasattr(CascadePlanner, '_walk_edges'); assert not hasattr(CascadePlanner, '_mget'); print('interfaces-ok')" && python -m pytest tests/unit/cascade/test_cascade_plan_table.py tests/unit/cascade/test_cascade_classification.py tests/unit/cascade/test_cascade_plan_injection.py -q</automated>
  </verify>
  <done>rapyer/cascade/planner.py no longer defines atraverse/_mget/_walk_edges; CascadePlanner's remaining methods and all module-level functions are byte-for-byte unchanged in behavior; the three plan-table/classification/injection test files still pass.</done>
</task>

<task type="auto">
  <name>Task 2: Delete test_cascade_planner.py and port its scenarios into the real Lua-apply tests</name>
  <files>tests/unit/cascade/test_cascade_planner.py, tests/unit/cascade/test_cascade_apply_lua.py</files>
  <action>
    Delete tests/unit/cascade/test_cascade_planner.py in full -- every one of its 17 tests calls `CascadePlanner().atraverse(...)`, which no longer exists after Task 1.

    In tests/unit/cascade/test_cascade_apply_lua.py:

    1. Remove the now-unused `from rapyer.cascade import CascadePlanner` import (nothing else in the file references `CascadePlanner` after the edits below).

    2. Repoint the 3 existing parity tests away from the live-oracle pattern. Each of these three tests already carries an explicit hardcoded expected-set assertion immediately below the oracle-based one -- delete only the oracle line and the oracle-comparison assertion, keep the hardcoded literal:
       - `test_shape1_chain_root_reaches_the_expected_prefix_of_the_chain_sanity`: delete `expected = set(await CascadePlanner().atraverse(root.key, CascadeChainRoot))` and the `assert refreshed == expected` line; keep `assert refreshed == {root.key, a.key, b.key, c.key}`. Update the preceding comment block to stop citing the (now-deleted) `test_multi_hop_chain_truncated_by_field_depth` planner test and the "client-side planner is the authority" line -- instead state inline why the set is `{root, a, b, c}`: CascadeChainRoot.head carries an explicit depth=2 override (refreshes child budget to 2), then CascadeChainNode.next is a blanket-decrementing hop at each subsequent established step (root->a budget 2, a->b budget 1, b->c budget 0, c->d exhausted -- d is never reached).
       - `test_depth0_shallow_root_extends_via_explicit_override_matches_planner_parity`: delete `expected = set(await CascadePlanner().atraverse(root.key, CascadeShallowRoot))` and `assert refreshed == expected`; keep `assert refreshed == set(all_keys)`. Adjust the trailing comment "must reach EXACTLY the CascadePlanner refresh set for this scalar shape-1 chain" to say "must reach EXACTLY the hand-derived expected set below". Consider renaming the test to drop "_matches_planner_parity" in favor of "_matches_hand_derived_expected_set" for accuracy (optional, keep if renaming feels disruptive to unrelated context).
       - `test_independent_sibling_depth_budgets_match_planner_parity`: delete `expected = set(await CascadePlanner().atraverse(root.key, CascadeMultiDepthRoot))` and `assert refreshed == expected`; keep `assert refreshed == {root.key, s1.key, s2.key, l1.key, l2.key, l3.key, l4.key}`. Adjust the comment similarly to drop the planner-parity framing in favor of stating the hand-derived reasoning already present in the comment (short_reach depth=1 reaches s1,s2; long_reach depth=3 reaches the whole l-chain).

       In all three cases the surviving hardcoded assertion is the pre-existing ground truth (it already matched the planner's output); only the redundant live-oracle comparison and its now-dead import are removed.

    3. Add two new test functions, following the existing file's pattern (`pytestmark` fixture already applies `setup_fake_redis_for_cascade_apply`; use the existing `_apply_cascade` helper), covering the two atraverse-only scenarios that have no equivalent coverage elsewhere in this file:

       - `test_blanket_opt_out_field_stops_traversal_despite_blanket_global`: uses `CascadeBlanketOptOut` (field carries `CascadeTTL(enabled=False)` overriding an otherwise-blanket-enabled `Meta.cascade_ttl(depth=2)`) and `CascadeBlanketLeaf`. Arrange: create a `CascadeBlanketLeaf`, then a `CascadeBlanketOptOut(child=leaf.key)`, persist both keys. Act: `_apply_cascade(fake_redis_client, root)`. Assert: `await fake_redis_client.ttl(root.key) > 0` and `await fake_redis_client.ttl(leaf.key) in (-1, -2)` (never reached -- the explicit per-field opt-out wins over the blanket global). Add both `CascadeBlanketOptOut` and `CascadeBlanketLeaf` to the file's import block from `tests.models.cascade_types`.

       - `test_nested_submodel_zero_hop_does_not_consume_depth_budget`: uses `CascadeNestedDepthRoot` (holder field carries explicit `CascadeTTL(depth=1)`) -> `CascadeBlanketNestedHolder` (unannotated `profile` field, a nested inline submodel -- shape 3, zero-hop) -> `CascadeBlanketNestedProfile` (unannotated `mentor` field, blanket-enabled via its own `Meta.cascade_ttl(depth=2)`) -> `CascadeBlanketLeaf`. Arrange: create a `CascadeBlanketLeaf` as mentor, a `CascadeBlanketNestedHolder(profile=CascadeBlanketNestedProfile(mentor=mentor.key))`, and a `CascadeNestedDepthRoot(holder=holder.key)`; persist root.key, holder.key, mentor.key. Act: `_apply_cascade(fake_redis_client, root)`. Assert: `refreshed = {key for key in (root.key, holder.key, mentor.key) if await fake_redis_client.ttl(key) > 0}` equals `{root.key, holder.key, mentor.key}` -- proving the zero-hop `profile` field never decremented the depth=1 budget inherited by `holder`, so the real FK hop into `mentor` still saw the untouched budget and was followed. Add a comment explaining this is the WR-01/D-06-shape-3 budget-non-consumption contract, ported from the deleted `test_nested_submodel_hop_does_not_consume_the_depth_budget` planner unit test. Add `CascadeNestedDepthRoot`, `CascadeBlanketNestedHolder`, `CascadeBlanketNestedProfile`, `CascadeBlanketLeaf` to the file's import block from `tests.models.cascade_types` (`CascadeBlanketLeaf` may already be added by the previous bullet -- import it once).

    Place both new tests in a new `# --- Ported from the deleted CascadePlanner unit tests (blanket opt-out + nested-submodel budget) ---` section near the end of the file, after the existing WR-02 test.
  </action>
  <verify>
    <automated>cd /Users/yedidyakfir/Documents/rapyer && test ! -f tests/unit/cascade/test_cascade_planner.py && ! grep -q "CascadePlanner" tests/unit/cascade/test_cascade_apply_lua.py && python -m pytest tests/unit/cascade -q</automated>
  </verify>
  <done>test_cascade_planner.py no longer exists; test_cascade_apply_lua.py has no CascadePlanner import or usage; the 3 parity tests assert only against hardcoded expected sets; the 2 newly ported tests (blanket opt-out, nested-submodel zero-hop budget non-consumption) pass against the real registered cascade_ttl_apply Lua script; the full tests/unit/cascade directory is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Test code -> registered Lua script | Tests invoke `cascade_ttl_apply` via `arun_sha` against `fake_redis_client`; the script's baked plan table is data derived from `tests/models/cascade_types.py` fixture classes at `register_scripts` time, not attacker-controlled input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-quick-01 | Tampering | rapyer/cascade/planner.py deletion scope | accept | This plan is a pure code/test removal + docstring update touching no production data path or external input; `_next_hop`/`_static_walk_fk_edges`/`build_cascade_plan`/`validate_cascade_ttl_targets` are left byte-for-byte behaviorally unchanged and covered by their own existing passing test files (test_cascade_plan_table.py, test_cascade_classification.py, test_cascade_plan_injection.py). |
| T-quick-02 | Repudiation (silent coverage loss) | tests/unit/cascade/test_cascade_planner.py deletion | mitigate | Every behavioral scenario the deleted file proved is re-verified in Task 2 against the real registered Lua script (stronger proof than the deleted Python-side BFS twin), not merely deleted and forgotten. |

No npm/pip/cargo package installs are introduced by this plan; the package-legitimacy gate does not apply.
</threat_model>

<verification>
1. `python -m pytest tests/unit -q` -- expect all green; count drops by 17 (deleted test_cascade_planner.py tests) and rises by 2 (newly ported tests), net -15 from the pre-plan baseline of 815 (documented here as the expected *direction*, not a hard gate -- if the actual delta differs, investigate before treating it as a regression).
2. `python -m pytest tests/integration/foreign_keys/test_cascade_ttl_apply.py -q` against real Redis Stack -- expect all green, unaffected by this plan (it never imported `CascadePlanner`).
3. `black --check rapyer/cascade/planner.py tests/unit/cascade/test_cascade_apply_lua.py` and `ruff check rapyer/cascade/planner.py tests/unit/cascade/test_cascade_apply_lua.py` -- both clean on the touched files.
4. `grep -rn "atraverse\|CascadePlanner()\._walk_edges\|CascadePlanner()\._mget" rapyer/ tests/` -- no matches remain anywhere in the repo.
</verification>

<success_criteria>
- `CascadePlanner.atraverse`, `_mget`, `_walk_edges` no longer exist; `CascadePlanner.__init__`/`_next_hop`/`_resolve_target_cls`/`_unwrap_nested_model_cls` and every module-level function in `rapyer/cascade/planner.py` are unchanged in behavior.
- `tests/unit/cascade/test_cascade_planner.py` is deleted.
- `tests/unit/cascade/test_cascade_apply_lua.py` has zero references to `CascadePlanner`, its 3 former parity tests assert only against hardcoded expected key sets, and it gained 2 new tests covering blanket opt-out and nested-submodel zero-hop budget non-consumption -- both passing against the real registered `cascade_ttl_apply` Lua script.
- `python -m pytest tests/unit -q` and `python -m pytest tests/integration/foreign_keys/test_cascade_ttl_apply.py -q` (real Redis) are both green.
- `black --check` and `ruff check` are clean on both touched source/test files.
</success_criteria>

<output>
Create `.planning/quick/260707-v5y-remove-dead-cascade-traversal/260707-v5y-SUMMARY.md` when done
</output>
